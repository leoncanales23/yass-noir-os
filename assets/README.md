# Yass Noir media layer

Editorial image assets are stored as Base64 text so the public landing can hydrate them at runtime through a lightweight loader. This keeps the first pilot fully self-contained in the repository while we iterate on the visual system.
