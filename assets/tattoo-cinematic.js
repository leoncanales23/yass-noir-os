(()=>{const section=document.querySelector('.noir-signature');if(!section)return;const media=section.querySelector('.signature-media');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=new IntersectionObserver(([entry])=>{if(entry.isIntersecting){section.classList.add('is-visible');reveal.disconnect()}},{threshold:.18});reveal.observe(section);if(reduced||!matchMedia('(pointer:fine)').matches)return;let frame=0;media.addEventListener('pointermove',event=>{cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{const rect=media.getBoundingClientRect();const x=(event.clientX-rect.left)/rect.width-.5;const y=(event.clientY-rect.top)/rect.height-.5;section.style.setProperty('--sig-x',`${(x*-10).toFixed(1)}px`);section.style.setProperty('--sig-y',`${(y*-7).toFixed(1)}px`)})});media.addEventListener('pointerleave',()=>{section.style.setProperty('--sig-x','0px');section.style.setProperty('--sig-y','0px')})})();