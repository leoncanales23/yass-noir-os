# Hero quality rule

The hero must never force a low-resolution source into a full-viewport crop.

Until a higher-resolution master is available, use the portrait as a framed editorial plate with restrained scale. A separate blurred atmosphere layer can fill the viewport so softness reads as intentional art direction rather than compression.
